info:
  name: Get Statistic Dashboard
  type: http
  seq: 17

http:
  method: GET
  url: http://localhost:8000/api/statistics
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
