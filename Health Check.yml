info:
  name: Health Check
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8000/api/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
